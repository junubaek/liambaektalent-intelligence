providers = ["python", "node"]

[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.build]
cmds = [
    "cd frontend_v2 && npm install --include=dev && npm run build",
    "pip install -r requirements.txt --no-cache-dir"
]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
