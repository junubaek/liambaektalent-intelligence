[build]
builder = "NIXPACKS"
buildCommand = "cd frontend_v2 && npm install && npm run build && cd .. && pip install -r requirements.txt --no-cache-dir"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
